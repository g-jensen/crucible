services:
  agent:
    image: ${IMAGE_NAME}
    working_dir: ${WORKING_DIR}
    volumes:
      - ./entrypoint.sh:/entrypoint.sh:ro
      - ${ABSOLUTE_RESULT_DIR}/opencode_config:/root/.config/opencode:rw
    command: ["/entrypoint.sh"]